## 1. Esquema de ratón

- [ ] 1.1 `player.gd`: visible por defecto, `_rmb_held` + drag-look (intento de captura), clics siempre por cursor, cruz oculta
- [ ] 1.2 `hud.gd` ×2 y `dialogue_panel.gd` ×1: cierres a `VISIBLE`; pista `RMB mirar` en `main.tscn`

## 2. Verificación y cierre

- [ ] 2.1 Sim PASS + modo visible por defecto
- [ ] 2.2 Navegador: sin drag no rota, drag rota, clic fija; 0 errores
- [ ] 2.3 Commit + push + rebuild + deploy
